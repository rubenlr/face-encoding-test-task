{
  "name": "face-encoding-test-task",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "tsc -p .",
    "re-build": "npm run clean && rm -rf ./node_modules && npm run build",
    "start": "npm run build && node dist/index.js",
    "dev": "nodemon --watch 'src/**/*.ts' --exec \"npx tsx src/server.ts\"",
    "smoke": "npx jest \"test/**/*.test.ts\"",
    "test:ci": "npx jest --colors --ci --coverage \"src/**/*.test.ts\"",
    "test": "npx jest --watch --colors --coverage \"src/**/*.test.ts\"",
    "clean": "rm -rf ./dist ./coverage"
  },
  "keywords": [],
  "author": "Rúben Lício Reis",
  "license": "unlicense",
  "description": "",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.677.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@testcontainers/localstack": "^10.13.2",
    "@types/jest": "^29.5.13",
    "@types/node": "^22.7.7",
    "@types/supertest": "^6.0.2",
    "jest": "^29.7.0",
    "nodemon": "^3.1.7",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "tslib": "^2.8.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3",
    "wait-port": "^1.1.0"
  }
}